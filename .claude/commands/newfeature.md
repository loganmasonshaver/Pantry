---
name: newfeature
description: Add a new feature or UI change to the Pantry app
---

A new feature or UI change needs to be made to the Pantry app: $ARGUMENTS

Follow this process:
1. Before writing any code, read the relevant existing files to understand current structure
2. Identify any packages that need to be installed and run the install command yourself
3. Implement the feature following Pantry conventions:
   - Pure black (#000000) background, white cards
   - React Native + Expo (iOS only)
   - Expo Router for navigation
   - Supabase for any data storage
   - OpenAI GPT-4o for anything AI-related
   - Superwall for anything subscription-gated
4. Update any related files (navigation, types, constants)
5. Do not ask me to run terminal commands manually — handle them yourself
6. Summarize what was added and any files changed

If anything is ambiguous, ask before building — not after.
